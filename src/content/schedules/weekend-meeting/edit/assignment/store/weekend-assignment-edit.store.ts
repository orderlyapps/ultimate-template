import { create } from "zustand";

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
type WeekendAssignmentEditState = {
  // Add state properties as needed
};

export const useWeekendAssignmentEditStore = create<WeekendAssignmentEditState>(() => ({
  // Initial state
}));
